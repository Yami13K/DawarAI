part of '../mobility_features.dart';

class PlacesPage extends StatelessWidget {
  const PlacesPage(List<Place> places, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}