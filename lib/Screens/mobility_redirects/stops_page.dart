part of '../mobility_features.dart';


class StopsPage extends StatelessWidget {
  const StopsPage(List<Stop> stops, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
