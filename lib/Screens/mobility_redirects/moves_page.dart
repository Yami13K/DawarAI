part of '../mobility_features.dart';

class MovesPage extends StatelessWidget {
  const MovesPage(List<Move> moves, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}